name: Deploy with Supabase

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          # No cache specified to avoid lock file issues

      # Determine package manager and install dependencies
      - name: Determine package manager and install dependencies
        run: |
          if [ -f "pnpm-lock.yaml" ]; then
            echo "Using pnpm for installation"
            npm install -g pnpm
            rm -f pnpm-lock.yaml
            pnpm install --no-frozen-lockfile
          else
            echo "Using npm for installation"
            rm -f package-lock.json
            npm install --no-package-lock --legacy-peer-deps
          fi

      - name: Install Supabase CLI
        run: |
          # Install Supabase CLI using their recommended method
          curl -fsSL https://cli.supabase.com/install.sh | sh
          # Add to PATH
          export PATH=/root/.supabase/bin:$PATH
          # Verify installation
          supabase --version || echo "Supabase CLI installation verification failed but continuing..."

      - name: Supabase Login
        run: |
          # Add Supabase to PATH
          export PATH=/root/.supabase/bin:$HOME/.supabase/bin:$PATH
          supabase login $SUPABASE_ACCESS_TOKEN

      - name: Link to Supabase project
        run: |
          # Add Supabase to PATH
          export PATH=/root/.supabase/bin:$HOME/.supabase/bin:$PATH
          if [ -z "$(find . -name 'supabase' -type d)" ]; then
            # Initialize Supabase if not already initialized
            supabase init
          fi
          supabase link --project-ref $PROJECT_ID --password $SUPABASE_DB_PASSWORD

      - name: Push Supabase migrations
        run: |
          # Add Supabase to PATH
          export PATH=/root/.supabase/bin:$HOME/.supabase/bin:$PATH
          supabase db push

      - name: Build application
        run: npm run build

      # Add deployment steps as needed, e.g.:
      # - name: Deploy to Vercel
      #   uses: amondnet/vercel-action@v20
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
